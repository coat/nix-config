{
  # falken is a graphical VM running awesome WM (configured at the system level
  # in machines/falken/configuration.nix). No extra NixOS modules are needed
  # from the user profile — home-manager desktop features are wired separately
  # via machines/falken/home.nix.
}
